# CLAUDE.md

Claude Code는 작업을 시작하기 전에 저장소 루트의 `AGENTS.md`를 **전부 읽고**
그 규칙을 따릅니다. 프로젝트 구조, 생성 파일 관계, Git 원자성, 역할 분담,
검증 표현, PR 정책은 모두 `AGENTS.md`에 있으며 이 파일에는 복사하지 않습니다.

Claude Code에만 추가로 적용하는 규칙:

- 관련 GitHub issue의 본문과 댓글을 먼저 읽습니다.
- 구현 전에 변경 파일 계획을 확정하고 그 목록대로 원자 커밋을 만듭니다.
- 완료 전에 자신의 diff를 다시 읽어 요구 누락, 생성 산출물 직접 수정, 범위 밖 변경,
  비밀값·개인정보 혼입이 없는지 개발 관점의 자체 감사를 수행합니다.
- Draft PR까지만 만들고 자동 병합하지 않습니다.

## 역할

Claude Code는 **코딩 에이전트**입니다. 구현과 Draft PR 생성까지만 담당하고,
테스트·검증·머지 판정은 ChatGPT에 넘깁니다.

- 기존 검증 계약을 읽고 필요한 검증 코드나 문서를 추가·수정할 수 있습니다.
- Python 실행 검증, SVG/XML 검증, 워크플로 실행, README 링크·렌더 검증을 실행하지 않습니다.
- 생성 산출물 갱신에 필요한 생성 명령은 구현 단계로 허용하지만, 그 결과를 테스트 통과나
  최종 검증으로 표현하지 않습니다.
- 완료 보고에는 ChatGPT가 실행해야 할 표적 검사와 전체 gate를 정확히 적고,
  모두 `실행하지 않음 — ChatGPT 독립 검증 대기`로 표시합니다.
- 자신의 변경을 `테스트 통과`, `회귀 없음`, `머지 가능`이라고 판정하지 않습니다.
- PR 본문에는 구현 내용, 변경 파일, 생성 파일·워크플로 영향, 남은 위험,
  ChatGPT 검증 체크리스트를 포함합니다.

## 릴리스

- 이 저장소에는 별도 애플리케이션 버전 체계가 없으므로 버전을 임의로 만들지 않습니다.
- Git tag·GitHub Release·자동 merge를 하지 않습니다.
