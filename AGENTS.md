# AGENTS.md

이 파일은 이 저장소에서 일하는 **모든 AI 도구의 공통·표준 지침**입니다.
Claude Code, ChatGPT, Codex 등 어떤 도구로 작업하든 여기 규칙을 먼저 따릅니다.

도구별 진입점(`CLAUDE.md`, `CHATGPT.md`)은 이 문서를 가리키는 짧은 안내일 뿐이며,
같은 내용을 복사해 두지 않습니다. 규칙이 바뀌면 공통 정책은 이 파일에서 고칩니다.

현재 진행 중 이슈나 일시적인 작업 상태처럼 금방 낡는 정보는 여기에 적지 않습니다.
그런 값은 열린 GitHub 이슈, 최근 PR, 실제 저장소 파일에서 읽습니다.

## 프로젝트

- 이 저장소는 GitHub 사용자 `pachir1su`의 **프로필 README 저장소**입니다.
- `README.md`가 GitHub 프로필 화면의 본문입니다.
- 기본 비주얼은 페이퍼톤인 크림 `#fffdf6`와 레드 `#c63b3b`를 중심으로 유지합니다.
- 문서와 사용자 안내는 한국어를 기본으로 작성합니다.

## 생성 파일과 외부 산출물

- `github-stats.svg`는 `generate_stats.py`가 생성합니다. SVG를 직접 수정하지 않고
  스크립트를 고친 뒤 재생성합니다.
- `github-metrics.svg`는 lowlighter/metrics 워크플로가 생성합니다.
- contribution snake는 Platane/snk 워크플로와 `output` 브랜치에서 생성됩니다.
- streak, activity graph 등 README의 일부 이미지는 외부 서비스 URL로 렌더링됩니다.
- `README.md`의 문구·레이아웃은 수작업으로 관리합니다.
- 생성 파일이나 외부 서비스 결과가 문제일 때는 원본 스크립트·워크플로·URL 계약을
  먼저 확인하고 결과물만 임의로 패치하지 않습니다.

## AI 역할 분담

이 저장소에서는 **개발과 독립 검증을 같은 AI 세션에 맡기지 않습니다.**

### Codex·Claude Code 등 코딩 에이전트

- 역할은 구현, 리팩터링, 검증 코드 작성, 문서 갱신, Draft PR 생성까지입니다.
- 자신이 만든 변경에 대해 Python 실행 검증, SVG 렌더 확인, 워크플로 실행,
  README 링크 검증 같은 테스트·검증 작업을 실행하지 않습니다.
- 기존 검증 계약을 읽거나 검증 코드를 작성하는 것은 가능합니다.
- 생성 산출물 갱신이 구현에 필요한 경우 생성 명령을 실행할 수 있지만 그 결과를
  최종 검증 통과로 표현하지 않습니다.
- 완료 보고에는 변경 파일, 구현 내용, 가정, 남은 위험, 실행하지 않은 검증을 정확히 적습니다.
- 독립 검증 없이 `테스트 통과`, `회귀 없음`, `머지 가능`이라고 판단하지 않습니다.
- Draft PR까지만 만들고 ChatGPT 검토 전에 merge하지 않습니다.

### ChatGPT

- 역할은 코딩 에이전트가 만든 PR·ZIP·diff의 독립 검토, 검증 명령 실행,
  SVG/XML·README·워크플로 확인, 머지 판단입니다.
- 코딩 에이전트의 완료 보고를 증거로 간주하지 않고 실제 diff와 결과를 확인합니다.
- 필요한 검증 범위를 정하고 실패 원인을 분석하며 수정이 필요하면 구체적인 재작업
  지시를 작성합니다.
- 제품 코드 구현은 기본 역할이 아닙니다. 사용자가 명시적으로 요청한 workflow 문서 수정,
  독립 검증 도구 작성, 또는 같은 작업에서 역할 변경을 명시한 경우만 예외로 합니다.

이 역할 분담은 사용자가 같은 작업에서 명시적으로 다르게 지시한 경우에만 예외로 합니다.

## 작업 시작 전에 읽을 것

작업을 시작하기 전에 최소한 다음을 실제로 읽습니다.

```text
AGENTS.md
README.md
CLAUDE.md 또는 CHATGPT.md 등 현재 도구의 진입점
관련 issue 본문 + 댓글
최근 관련 PR
generate_stats.py
관련 .github/workflows 파일
```

- 업로드된 ZIP의 설명이나 이전 작업의 완료 보고만 믿지 않고 실제 파일을 읽습니다.
- 이슈 본문뿐 아니라 댓글도 모두 확인합니다.
- 닫힌 이슈는 사용자가 명시적으로 요청하지 않는 한 근거로 삼지 않습니다.
- PowerShell에서 한글 파일은 `Get-Content -Encoding UTF8`로 읽습니다.

## 사용자 요구 해석

- 사용자가 `예를 들어`라고 든 내용은 예시이며 같은 원인의 인접 항목도 확인합니다.
- 범위는 요청한 결함과 같은 원인·같은 패턴까지만 확장합니다.
- README의 문구·디자인처럼 결과가 사용자 취향에 크게 좌우되고 저장소 근거로 결정할 수
  없는 선택은 구현 전에 확인합니다.
- 실제 비밀값, 데이터 삭제, 비가역 작업, 서로 배타적인 제품 요구처럼 결과가 크게
  달라지는 선택만 구현 전에 질문합니다.
- 요구가 명확하고 되돌릴 수 있으면 저장소 근거에 따라 진행하고 결과를 보고합니다.

## 구현 계획 품질

구현 전 계획에는 최소한 다음을 포함합니다.

```text
정확한 파일
현재 문제
수정할 데이터 또는 control flow
생성 파일 관계
워크플로 영향
비밀값 경계
검증 계약
렌더링 QA 항목
```

자연어 요구를 반복하는 것만으로 계획을 대신하지 않습니다.

## 코드와 문서 변경

- 기존 구조와 네이밍 스타일을 우선 따릅니다.
- Python 변수와 함수는 기존 코드 스타일을 우선하며 새 이름은 `camelCase`를 기본으로 합니다.
- 불필요한 리팩터링과 범위 밖 변경을 피합니다.
- `README.md` 문구·레이아웃은 사용자 요구 또는 기존 디자인 방향과 맞는 경우에만 변경합니다.
- `github-stats.svg`, `github-metrics.svg`, snake 등 생성 산출물을 직접 수정하지 않습니다.
- 생성 산출물과 원본 스크립트를 중복 편집하지 않습니다.

## 비밀과 토큰

- 비밀값, 실제 토큰, 비밀번호, 개인 설정을 출력하거나 커밋하지 않습니다.
- `GITHUB_TOKEN`, `METRICS_TOKEN` 등은 저장소 또는 Actions secret으로 취급합니다.
- 토큰 값이 필요하거나 바꿔야 하면 값을 추측하거나 문서에 적지 않고 필요한 secret 이름과
  설정 위치만 안내합니다.
- 실제 계정 ID나 운영용 secret 값을 PR 본문·문서·로그 예시에 넣지 않습니다.

## Git 원자성

- **모든 PR에서 변경 파일 수와 커밋 수는 정확히 같아야 합니다.**
- 커밋 하나는 정확히 파일 하나만 변경합니다.
- 변경 파일 하나는 정확히 커밋 하나에만 등장해야 합니다.
- 같은 파일을 여러 커밋으로 나눠 수정하지 않습니다.
- 추가·수정·삭제·rename 파일과 생성 산출물도 각각 별도 커밋으로 취급합니다.
- `AGENTS.md`, `CLAUDE.md`, `CHATGPT.md`도 각각 별도 커밋입니다.
- PR 생성 전에 기록을 정리하고, 최종 변경 파일 수와 커밋 수가 같은지 확인합니다.
- 커밋 메시지는 `<type>: <short description>` 형식이며 `feat`, `fix`, `refactor`,
  `docs`, `chore` 중 적절한 type을 사용합니다.

브랜치 이름은 사용하는 코딩 에이전트의 명시적 규칙이 있으면 그 규칙을 따르고,
랜덤 문자열 브랜치는 만들지 않습니다.

## 이슈와 PR

- PR 댓글의 `ㅇㅇ`는 곧 머지하겠다는 뜻이며 추가 작업 지시가 아닙니다.
- PR 본문에 관련 이슈 번호와 구현 범위를 명확히 연결합니다.
- 구현 내용, 변경 파일, 생성 파일·워크플로 영향, 남은 위험, ChatGPT 검증 체크리스트를
  PR 본문에 포함합니다.
- 코딩 에이전트는 Draft PR까지만 만들고 자동 merge하지 않습니다.
- 사용자가 요청하지 않은 주기적 PR 재검증이나 셀프 체크인을 예약하지 않습니다.

## 독립 검증 계약

코딩 에이전트는 아래 검증을 실행하지 않고 ChatGPT 검증 체크리스트에 남깁니다.
생성 산출물 갱신 자체가 구현에 필요한 경우 생성 명령은 실행할 수 있습니다.

```text
python -m py_compile generate_stats.py
python generate_stats.py
```

ChatGPT는 변경 범위에 따라 다음을 독립적으로 확인합니다.

- `python -m py_compile generate_stats.py`
- `generate_stats.py`의 입력·출력 계약과 secret 사용 방식
- `github-stats.svg` XML 파싱과 렌더 구조
- README의 내부·외부 링크, 이미지 URL, 배지 URL
- `.github/workflows` YAML과 생성 산출물 경로
- 생성 파일이 원본 스크립트·워크플로와 일치하는지

실제 토큰이 필요한 실행은 secret을 노출하지 않는 방식으로만 수행하며,
검증하지 못한 항목은 반드시 `실행하지 못함` 또는 `실행하지 않음`으로 표시합니다.

## 릴리스

- 이 저장소에는 별도 애플리케이션 버전 체계가 없으므로 임의로 버전을 만들지 않습니다.
- Git tag와 GitHub Release를 임의로 만들지 않습니다.
- 자동 merge하지 않습니다.
- README와 생성 통계의 갱신은 배포 버전이 아니라 프로필 콘텐츠 변경으로 취급합니다.
